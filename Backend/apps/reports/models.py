# Reports (manager dashboard, module 13) is computed from other apps' data and
# has no models of its own. Phase-4 analytics endpoints + PDF/CSV export will
# live in this app (views/services). Placeholder kept so the app loads cleanly.
