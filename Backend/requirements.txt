# Clinic Management System — Backend dependencies
# Verified for Python 3.14.0 on Windows (June 2026). Always install from PyPI wheels.

# --- Core framework ---
Django==5.2.8
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1

# --- API infrastructure ---
django-cors-headers==4.9.0
django-filter==25.2
django-environ==0.13.0

# --- Files / PDF ---
Pillow==12.2.0          # cp314 wheel — image uploads + logo compositing
reportlab==4.5.1        # pure-Python — ALL PDFs (prescriptions, reports)

# --- Audit (optional history on MedicalRecord only) ---
django-simple-history==3.11.0

# --- Optional SMS / WhatsApp (feature-flagged off by default) ---
twilio==9.10.9          # SMS + WhatsApp via Twilio; feature-gated by SMS_ENABLED / WHATSAPP_ENABLED

# --- AI Scribe (apps/ai_scribe): voice session -> transcript -> structured draft ---
# Speech-to-text. faster-whisper runs Whisper large-v3 locally via CTranslate2
# (4x faster, ~1/4 the memory of openai-whisper, same accuracy). Best open option
# for Egyptian Arabic. Bundles its own audio decoding (PyAV) — no separate ffmpeg.
# Verified to install on Python 3.14 (cp314 wheels resolved June 2026).
faster-whisper==1.2.1   # pulls ctranslate2, av (audio decode), onnxruntime, tokenizers
# Structured extraction. Google Gemini has a genuinely free API tier (free key at
# aistudio.google.com, no card) and strong Arabic. Provider is pluggable in
# apps/ai_scribe/services/extraction.py — swap for Groq/OpenAI by editing one module.
google-genai==2.9.0

# --- Dev / test ---
pytest==8.4.1
pytest-django==4.11.1
